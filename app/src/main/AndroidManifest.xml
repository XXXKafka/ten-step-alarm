<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Exact alarms: declared on Android 12+ (SCHEDULE_EXACT_ALARM needs a user grant;
         USE_EXACT_ALARM is auto-granted for alarm clock apps). -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <!-- Full-screen alarm UI (Android 14+ full-screen intent permission). -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <!-- Runtime notification permission on Android 13+. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Step counting (TYPE_STEP_COUNTER on Android 10+). -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />
    <!-- Play the default alarm sound on Xiaomi/MIUI, which stores it behind a
         media URI that needs the audio permission. -->
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <!-- QR-challenge camera scanning. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- Camera is optional: QR challenge falls back to steps without it. -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:name=".TenStepApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.TenStepAlarm">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Full-screen ringing UI, shown above the lock screen. It runs in
             its own task (singleInstance) so a lock-screen launch never joins
             the main activity's background task, which HyperOS/MIUI would
             otherwise keep hidden behind the keyguard. -->
        <activity
            android:name=".alarm.AlarmRingingActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:launchMode="singleInstance"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:theme="@style/Theme.TenStepAlarm" />

        <service
            android:name=".alarm.AlarmRingingService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="alarm_ringing" />
        </service>

        <service
            android:name=".alarm.AlarmMonitorService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="alarm_monitor" />
        </service>

        <receiver
            android:name=".alarm.AlarmReceiver"
            android:exported="false" />

        <receiver
            android:name=".alarm.BootReceiver"
            android:exported="true"
            android:permission="android.permission.RECEIVE_BOOT_COMPLETED">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Re-schedules all alarms once the exact-alarm permission is granted. -->
        <receiver
            android:name=".alarm.ExactAlarmPermissionReceiver"
            android:exported="true"
            android:permission="android.permission.SCHEDULE_EXACT_ALARM">
            <intent-filter>
                <action android:name="android.app.action.SCHEDULE_EXACT_ALARM_PERMISSION_STATE_CHANGED" />
            </intent-filter>
        </receiver>

        <!-- Fires the timer end notification (sound) even when the app is closed. -->
        <receiver
            android:name=".timer.TimerReceiver"
            android:exported="false" />
    </application>
</manifest>
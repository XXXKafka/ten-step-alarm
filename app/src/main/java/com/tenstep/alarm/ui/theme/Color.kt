package com.tenstep.alarm.ui.theme

import androidx.compose.ui.graphics.Color

// Neutral surfaces used by the seed-derived palettes.
val LightBackground = Color(0xFFFAFDFB)
val LightOnBackground = Color(0xFF191C1B)
val LightSurface = Color(0xFFFAFDFB)
val LightOnSurface = Color(0xFF191C1B)
val LightSurfaceVariant = Color(0xFFDAE5E1)
val LightOnSurfaceVariant = Color(0xFF3F4946)
val LightOutline = Color(0xFF6F7976)
val LightSurfaceContainer = Color(0xFFEFF1EF)

val DarkBackground = Color(0xFF191C1B)
val DarkOnBackground = Color(0xFFE0E3E1)
val DarkSurface = Color(0xFF191C1B)
val DarkOnSurface = Color(0xFFE0E3E1)
val DarkSurfaceVariant = Color(0xFF3F4946)
val DarkOnSurfaceVariant = Color(0xFFBEC9C5)
val DarkOutline = Color(0xFF899390)
val DarkSurfaceContainer = Color(0xFF252928)

/** Default seed color (teal). */
val TealPrimary = Color(0xFF006A60)

/**
 * Curated seed colors for the "Preset" theme color picker.
 * Each seed is expanded into a full light/dark Material 3 scheme.
 */
val ThemePresetSeeds: List<Color> = listOf(
    Color(0xFF006A60), // Teal
    Color(0xFF0B57D0), // Blue
    Color(0xFF5B34C4), // Purple
    Color(0xFF2E7D32), // Green
    Color(0xFFE8710A), // Orange
    Color(0xFFB3261E), // Red
    Color(0xFFC2185B), // Pink
    Color(0xFF455A64)  // Blue grey
)